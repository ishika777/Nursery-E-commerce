<% layout("layouts/boilerplate") %>
<%- include("../includes/subNav.ejs") %>

<div class="container">
    <h3 class="category-heading">Flowers</h3>
    <% cnt = 0 %>
    <div class="container-wrapper row row-cols-lg-3 row-cols-md-2 row-cols-sm-1 mt-3 mb-3">
        <% for (let product of flowerPlants) { %>
            <% cnt++ %>
            <a href="/home/<%= product._id %>" class="product-link">
                <div class="card col product-card">
                    <img src="<%= product.image.url %>" class="card-img-top" alt="<%= product.name %>">
                    <div class="card-img-overlay"></div>
                    <div class="card-body">
                        <p class="card-text">
                            <b><%= product.name %></b> <br>
                            <span>Price: $<%= product.price.toFixed(2) %></span>
                        </p>                        
                    </div>
                </div>
            </a>
        <% } %>
        <% if(cnt == 0){ %>
            <div>
                <p>No Flowers Available!</p>
            </div>
        <% } %>
    </div>
</div>
